{
  "name": "devsphere",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "npm run build && NODE_ENV=production npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
